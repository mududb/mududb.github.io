# mududb.github.io

MuduDB 的文档站点仓库。
## 内容

- `docs/` — 已构建好的 HTML 文档（当前版本 `v0.1`，含 `en` 与 `zh_cn` 两种语言）。
- `docs/index.html` — 站点入口。
- `docs/docs-pages.json` — 各版本、各语言下的文档页面清单。

## 说明

- 本仓库用于托管和发布文档（GitHub Pages）。
- 文档源文件不在本仓库中维护，请勿在此直接修改文档内容；文档更新请前往对应的文档源仓库，生成后同步到本仓库。
